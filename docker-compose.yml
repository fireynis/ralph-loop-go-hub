services:
  ralph-hub:
    build: .
    container_name: ralph-hub
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      TZ: America/Toronto
    volumes:
      - ./config.yaml:/etc/ralph-hub/config.yaml:ro
